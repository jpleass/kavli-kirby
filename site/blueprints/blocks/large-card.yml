name: Large Card
preview: fields
tabs:
  content:
    fields:
      card: fields/card
  styles:
    fields:
      style:
        type: select
        label: Style
        default: default
        options:
          - default
          - transparent
